
script#profile-myappointments-tpl(type="text/template")
  .ui.celled.list
    |<% _.each(items, function(item){ %>
    .item
      img.ui.avatar.image(src!="<%= item.consultant_avatar %>")
      .content
        .header <%= item.consultant_name %>
        .description 
          .label 时间 <%= (new Date(item.date)).toLocaleString() %>
          .label 状态 <%= item.state %>
          a(href!="<%= item.consultant_qqAddress %>") QQ聊天
    |<% }); %>

script#profile-mycourses-tpl(type="text/template")
  .ui.divided.list
    |<% _.each(items, function(item){ %>
    .item
      i.map.marker.icon
      .content
        .header <%= item.consultant_name %>
        .description 
          .label 时间 <%= item.date %>
          .label 状态 <%= item.state %>
          a(href!="<%= item.qqAddress %>") QQ聊天
    |<% }); %>

script#profile-mytests-tpl(type="text/template")

script#profile-myarticles-tpl(type="text/template")

script#profile-info-tpl(type="text/template")
  .ui.piled.segment
    img.ui.small.image(src!="<%= avatar%>")
    h3.ui.header <%= username %>
    p.description <%= about %>
    form.ui.form  
      .field
        label 邮箱：    
        .ui.left.icon.input
          input(type="text", name="email", value!="<%= email %>", required)
          i.mail.icon
      .field
        label 性别：   
        select.ui.dropdown(name="gender")
          option(value="male") 男
          option(value="female") 女
      .field
        label 年龄：    
        .ui.left.icon.input
          input(type="text", name="age", value!="<%= age %>", required)
          i.user.icon
      .field
        label 地址：    
        .ui.left.icon.input
          input(type="text", name="address", value!="<%= address %>", required)
          i.home.icon
      .field
        label 手机：    
        .ui.left.icon.input
          input(type="text", name="mobile", value!="<%= mobile %>", required)
          i.call.icon
      .field
        label QQ：    
        .ui.left.icon.input
          input(type="text", name="qq", value!="<%= qq %>", required)
          i.call.icon
      .field
        label QQ 地址：    
        .ui.left.icon.input
          input(type="text", name="qqAddress", value!="<%= qqAddress %>", alt="外链地址，可不填", required)
          i.call.icon
      .field
        label 固定电话：  
        .ui.left.icon.input
          input(type="text", name="landline", value!="<%= landline %>", required)
          i.call.icon
      .ui.horizontal.divider
      .ui.left.icon.input
        input.ui.submit.button(type="submit", value="保存")
        i.save.icon
    .ui.horizontal.divider
